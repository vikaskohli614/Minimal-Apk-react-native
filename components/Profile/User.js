import React from 'react';
import {View, StyleSheet, Text} from 'react-native';

const User = () => {
    return (
        <View>
            <Text>user</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default User;
